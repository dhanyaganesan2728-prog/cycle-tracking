body {
  margin: 0;
  font-family: 'Segoe UI', sans-serif;
  background-color: #0b1f3a; /* matched background */
  color: white;
}

.top-nav {
  display: flex;
  justify-content: space-between;
  align-items: center;
  background-color: #141c2c; /* matched header color */
  padding: 10px 30px;
}

.nav-left h2 {
  color: #00f2fe; /* same cyan-blue heading */
  font-size: 24px;
  margin: 0;
}

.nav-right span {
  margin-left: 20px;
  color: #cbd5e1;
  font-size: 14px;
}

.profile-pic {
  width: 30px;
  height: 30px;
  border-radius: 50%;
  margin-left: 10px;
  border: 2px solid #00f2fe;
}

.editor-container {
  display: flex;
  height: calc(100vh - 60px);
}

.editor-main {
  flex: 1;
  display: flex;
  flex-direction: column;
  padding: 20px;
}

.code-box {
  flex: 1;
  background-color: #1e293b; /* slightly lighter than bg */
  border: 2px solid #334155;
  border-radius: 10px;
  padding: 15px;
  font-family: 'Courier New', monospace;
  font-size: 15px;
  color: white;
  overflow: auto;
  margin-bottom: 20px;
}

.output-box {
  height: 150px;
  background-color: #112233; /* subtle dark box */
  border: 2px solid #334155;
  border-radius: 10px;
  padding: 15px;
  font-family: 'Courier New', monospace;
  color: #cbd5e1;
  overflow-y: auto;
}

.editor-sidebar {
  width: 220px;
  background-color: #112233; /* same as filters/sidebar */
  padding: 20px;
  border-right: 1px solid #1e2e3c;
}

.search-input {
  width: 100%;
  padding: 8px 10px;
  border-radius: 8px;
  border: none;
  background-color: #1e293b;
  color: white;
  margin-bottom: 20px;
  font-size: 14px;
}

.language-list {
  list-style: none;
  padding: 0;
  margin: 0;
  display: flex;
  flex-direction: column;
  gap: 10px;
}

.language-list input[type="radio"] {
  display: none;
}

.language-list label {
  font-weight: bold;
  padding: 8px 12px;
  border-radius: 8px;
  cursor: pointer;
  transition: background 0.3s, color 0.3s;
  background-color: #1e293b;
  color: white;
  border: 1px solid #334155;
}

.language-list label:hover {
  background-color: #00f2fe;
  color: #0b1f3a;
}

.language-list input[type="radio"]:checked + label {
  background-color: #00f2fe;
  color: #0b1f3a;
}
